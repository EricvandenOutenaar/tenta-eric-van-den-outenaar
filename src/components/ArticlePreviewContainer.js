import React from "react";
import { ArticlePreviewContainerWrapper } from "../elements";

export const ArticlePreviewContainer = ({ children }) => {
  return (
    <ArticlePreviewContainerWrapper>{children}</ArticlePreviewContainerWrapper>
  );
};
